@page "/videos"
@using MyGithubPage.Models

<h3>视频分享</h3>
<!-- Buttons -->
<button class="btn btn-primary m-1" @onclick='() => SelectContent("第一讲", "https://drive.google.com/file/d/1fzLlgiKXyh1zlYnSlIHjWEJvGmSrjOKp/preview", "https://drive.google.com/uc?export=download&id=1fzLlgiKXyh1zlYnSlIHjWEJvGmSrjOKp")'>
    蒙神祝福的婚姻 第一讲
</button>

<button class="btn btn-secondary m-1" @onclick='() => SelectContent("第二讲", "https://drive.google.com/file/d/1YYAGxnMAZn9LthCMFf8aY5OpWbt60tGz/preview", "https://drive.google.com/uc?export=download&id=1YYAGxnMAZn9LthCMFf8aY5OpWbt60tGz")'>
    蒙神祝福的婚姻 第二讲
</button>

<!-- Conditional Display Section -->
@if (!string.IsNullOrEmpty(selectedVideoUrl))
{
    <div class="mt-4">
        <h4>Now Playing: @selectedTitle</h4>
        
        <iframe src="@selectedVideoUrl" width="640" height="480" sandbox="allow-scripts allow-same-origin"></iframe>
        
        <!-- Download Link -->
        <div class="mt-2">
            <a class="btn btn-success" href="@selectedDownloadLink" download>
                Download Video Resource
            </a>
        </div>
    </div>
}

@code {
    private List<MediaItem> MediaItems = new List<MediaItem>();

    private string? selectedTitle;
    private string? selectedVideoUrl;
    private string? selectedDownloadLink;

    private void SelectContent(string title, string videoUrl, string downloadLink)
    {
        MediaItem mediaItem = new MediaItem
        {
            Title = title,
            Url = videoUrl,
            //DownloadLink = downloadLink
        };

        MediaItems.Add(mediaItem);

        selectedTitle = title;
        selectedVideoUrl = videoUrl;
        selectedDownloadLink = downloadLink;
    }
}

